# Inference Module

Prediction and submission output helpers.

## Files

- `predict_24h.py`: enforces non-negative and `pred_p90 >= pred_power`.
- `submission_writer.py`: validates and writes `predictions.csv`.
